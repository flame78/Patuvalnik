<Page
    x:Class="Patuvalnik.Pages.OptionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Patuvalnik.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Button x:Name="InviteFriendsButton" Content="Invite friends"
                Grid.Row="0"/>
        <Button x:Name="RateButton" Content="Rate"
                Grid.Row="1"/>
        <Button x:Name="LogOutButton" Content="Log out"
                Grid.Row="2"/>
    </Grid>
</Page>
